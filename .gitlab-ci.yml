image: ruby:2.4.2

stages:
  - build
  - test
  - publish
  - review
  - stage
  - production

variables:
  DATABASE_URL: 'mongodb://mongo/user_posts'
  TAG_LATEST: $CI_REGISTRY_IMAGE/$CI_COMMIT_REF_NAME:latest
  TAG_COMMIT: $CI_REGISTRY_IMAGE/$CI_COMMIT_REF_NAME:$CI_COMMIT_SHORT_SHA

# before_script:
# - cd reddit
# - bundle instal

# build_job:
#   stage: build
#   script:
#     - echo 'Building'

# test_unit_job:
#   stage: test
#   services:
#     - mongo:latest
#   script:
#     - ruby simpletest.rb

# test_integration_job:
#   stage: test
#   script:
#     - echo 'Testing 2'

publish:
  image: docker:19-dind
  stage: publish
  services:
    - name: docker:19-dind
      entrypoint: ["dockerd-entrypoint.sh", "--tls=false"]
  before_script:
    - echo $TAG_LATEST
    - echo $CI_REGISTRY_IMAGE
    - echo $CI_BUILD_TOKEN
    - echo $CI_REGISTRY
  script:
    - export DOCKER_HOST=tcp://127.0.0.1:2375
    - export DOCKER_TLS_CERTDIR=
    - cd reddit
    - docker build -t "$TAG_LATEST" .
    # - docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN $CI_REGISTRY
    - docker push "$TAG_LATEST"

deploy_dev_job:
  image: alpine:latest
  stage: review
  tags:
    - deployment
  script:
    - chmod og= $ID_RSA
    - apk update && apk add openssh-client
    # - ssh -i $ID_RSA -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "docker login -u gitlab-ci-token -p $CI_BUILD_TOKEN $CI_REGISTRY"
    - ssh -i $ID_RSA -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "docker pull $TAG_LATEST"
    - ssh -i $ID_RSA -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "docker container rm -f mongo || true"
    - ssh -i $ID_RSA -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "docker container rm -f reddit || true"
    - ssh -i $ID_RSA -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "docker network rm -f reddit || true"
    - ssh -i $ID_RSA -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "docker network create reddit || true"
    - ssh -i $ID_RSA -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "docker run -d --network=reddit -p 27017:27017 --name mongo mongo:latest"
    - ssh -i $ID_RSA -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "docker run -d --network=reddit -p 80:80 -e DATABASE_URL=mongo:27017 --name reddit $TAG_LATEST"
  environment:
    name: review/$CI_COMMIT_REF_NAME
    url: http://$SERVER_IP
  only:
  - branches
  except:
  - master

staging:
  stage: stage
  when: manual
  only:
    - /^\d+\.\d+\.\d+/
  script:
    - echo 'Deploy'
  environment:
    name: beta
    url: http://beta.example.com

production:
  stage: production
  when: manual
  only:
    - /^\d+\.\d+\.\d+/
  script:
    - echo 'Deploy'
  environment:
    name: production
    url: http://example.com
